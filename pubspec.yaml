name: extensions_core
description: >-
  A dependency-light collection of Dart and Flutter extension methods for
  strings, numbers, dates, collections, colors, widgets, navigation, and more.
version: 0.1.0
homepage: https://github.com/LeanQChris/extensions
repository: https://github.com/LeanQChris/extensions
issue_tracker: https://github.com/LeanQChris/extensions/issues
topics:
  - dart
  - flutter
  - extensions
  - utilities

environment:
  sdk: ^3.6.0
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
